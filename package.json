{
  "name": "root",
  "private": true,
  "scripts": {
    "build:semaphore-contracts": "npx lerna exec --scope semaphore-contracts npm run compile",
    "build:maci-contracts": "npx lerna run build --scope maci-contracts",
    "test:semaphore-contracts": "npx lerna run test --scope semaphore-contracts",
    "test:maci-contracts": "npx lerna run test --scope maci-contracts",
    "hardhat:node": "npx lerna exec --scope semaphore-contracts npm run start",
    "hardhat:funds": "npx ts-node  scripts/hardhat-deploy.ts",
    "deploy:semaphore": "npx lerna exec --scope semaphore-contracts -- 'yarn compile && yarn deploy --network localhost' && npx ts-node scripts/copyDeployedSemaphoreAddress.ts",
    "deploy:maci": "npx lerna exec --scope maci-contracts -- 'pnpm run compileSol && pnpm run deploy --network localhost' ",
    "download-zkeys:test": "npm run --prefix maci download-zkeys:test"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "lerna": "^8.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}